using UnityEngine;
using System.Collections;

public class Rally : Abilities {
	/*private const string aName = "Rally!";
	private const int rangeX = 3;
	private const int rangeY = 3;
	private const int baseDamage = 0;
	private const int tier = 2;
	private const string character = "Warrior";
	private const int coolDown = 5;
	private const int reduction = 2; //cooldown reducted

	public Rally(int cooldownReduction) : base(aName, rangeX, rangeY, baseDamage, tier, character, coolDown){

	}
	public int cooldownReduction{
		get{ return cooldownReduction; }
		set{cooldownReduction = reduction;}
	}*/
}
