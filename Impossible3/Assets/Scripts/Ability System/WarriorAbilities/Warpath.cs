using UnityEngine;
using System.Collections;

public class Warpath : Abilities {
	/*private const string aName = "Warpath!";
	private const int rangeX = 1;
	private const int rangeY = 10;
	private const int baseDamage = 40;
	private const int tier = 2;
	private const string character = "Warrior";
	private const int coolDown = 5;
	private const int stanceDuration = 3; //duration of Counter
	private const bool stance = true; //whether is in the counter stance
	private const int damageReductionBuff = 10;
	private const int damageBoost = 10;

	public Warpath(bool state, int duration, int damageReduction, int damageBoosted) : base(aName, rangeX, rangeY, baseDamage, tier, character, coolDown){

	}
	public bool state{
		get{return state;}
		set{ state = stance; }
	}
	public int duration{
		get{ return duration; }
		set{duration = stanceDuration;}
	}
	public int damageReduction{
		get{ return damageReduction; }
		set{damageReduction = damageReductionBuff;}
	}
	public int damageBoosted{
		get{ return damageBoosted; }
		set{damageBoosted = damageBoost;}
	}*/
}
