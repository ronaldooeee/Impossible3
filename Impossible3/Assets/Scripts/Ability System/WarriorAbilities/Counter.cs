using UnityEngine;
using System.Collections;

public class Counter : Abilities {
	/*private const string aName = "Counter!";
	private const int rangeX = 0;
	private const int rangeY = 0;
	private const int baseDamage = 0;
	private const int tier = 1;
	private const string character = "Warrior";
	private const int coolDown = 5;
	private const int stanceDuration = 3; //duration of Counter
	private const bool stance = true; //whether is in the counter stance
	private const int returnDamage = 40;

	public Counter(bool state, int duration, int returnDamageDone) : base(aName, rangeX, rangeY, baseDamage, tier, character, coolDown){

	}
	public bool state{
		get{return state;}
		set{ state = stance; }
	}
	public int duration{
		get{ return duration; }
		set{duration = stanceDuration;}
	}
	public int returnDamageDone {
		get{ return returnDamageDone; }
		set{ returnDamageDone = returnDamage; }
	}*/
}
